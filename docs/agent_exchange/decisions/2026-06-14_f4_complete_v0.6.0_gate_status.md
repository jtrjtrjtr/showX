# F4 (ShowX-7) impl complete — v0.6.0, automated gate green, visual pending Jindřich

**Date:** 2026-06-14 ~05:24 CEST
**Status:** F1+F2+F3+F4 all impl accepted. v0.6.0 DMG built. Automated gate GREEN. Visual/audio gate → Jindřich GUI session (batched).

## F4 — 8/8 impl accepted (clean, all r1 except 008 r2)
001 caller_lines model+UI · 002 deterministic generator+aggregation · 003 LLM draft · 004 ElevenLabs Node client+voice clone (REST, no new dep) · 005 rehearsal pre-gen→.showx/media · 006 playback engine (renderer Web Audio + cue-lights subscribe) · 007 interrupt <200ms · 008 intercom out (r2). Zero blocks.

## Cumulative (F1+F2+F3+F4)
- 40/41 impl tasks accepted across 4 bundles + F4 design doc, autonomous.
- Tests: 1509 (v0.2.1) → 1666 (F1) → 1842 (F2) → 1977 (F3) → **2133 (F4)**. +624 tests.
- v0.6.0-arm64 DMG builds clean, no node:* leak.

## Automated gate (Architect) — GREEN
- typecheck clean (5 pkg) · pnpm test 2133/2133 · build clean, no leak · DMG v0.6.0 builds, asar correct.

## RESCUE (recurring) — root package.json clobbered AGAIN
2nd occurrence (after B006-001): a Forge task editing package.json deps rewrote it as a minimal app package.json, stripping scripts/devDeps → pnpm test/build broke. Restored full workspace root + 0.6.0. **Added guardrail** `tests/unit/packageJsonIntegrity.test.ts` (asserts name/main/scripts/devDeps/workspaces) so the suite (Critic) catches this before accept going forward. Lesson locked in test.

## Visual/audio gate — Jindřich (headless + no audio hw)
Architect context = no display + no audio output. Voice quality, real intercom device, hearing standby/GO = Jindřich's GUI session. Checklist: `docs/JINDRICH_TEST_CHECKLIST_v0.6.0_F4.md` (+ v0.5.0 for F1-F3). DMG: dist-electron/ShowX-0.6.0-arm64.dmg.

## Open for Jindřich
1. Run v0.6.0 test (F1-F4 batched). Needs ElevenLabs + Anthropic keys for full F4 (graceful without).
2. ElevenLabs cloning quality/cost verdict (docs/dev/elevenlabs_tos_cost_note.md).
3. Apple cert → B006-002 signing.
4. Next direction: LTC bundle? pricing? product web? (roadmap F5/F6 territory.)

## Not closing yet
B007-009 gate = in_progress (automated done, visual pending). Bundle closes + v0.6.0 tag on Jindřich's pass. Runners disabled (no queued tasks).
