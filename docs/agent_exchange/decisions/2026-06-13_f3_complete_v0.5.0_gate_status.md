# F3 (ShowX-6) impl complete — v0.5.0, automated gate green, visual pending Jindřich

**Date:** 2026-06-13 ~23:44 CEST
**Status:** F1+F2+F3 all impl accepted. v0.5.0 DMG built. Automated gate GREEN. Visual/interactive gate → Jindřich GUI session (batched B004-012 + B005-010 + B006-012).

---

## F3 — 11/11 impl accepted (clean)

001 DMG fix (architect boot-verify) · 002 signed pipeline (graceful unsigned; needs cert) · 003 device health · 004 device feedback · 005 multi-dest primary+backup · 006 pre-show wizard (r2) · 007 cue-lights protocol+SM · 008 cue-lights operator+ack · 009 SHOW proposals · 010 authority/registry (r2) · 011 F4 design (architect). Zero blocks (after 001 rescue), zero round-4+.

## Cumulative (F1+F2+F3)
- 32/33 impl tasks accepted across 3 bundles + 1 architect design doc, in one extended session.
- Tests: 1509 (v0.2.1) → 1666 (F1) → 1842 (F2) → **1977 (F3)**. +468 tests.
- v0.5.0-arm64 DMG builds cleanly, asar correct.

## Automated gate (Architect, this context) — GREEN
- `pnpm -r typecheck` clean (5 projects)
- `pnpm test` 1977/1977
- `pnpm build` clean, no node:* leak
- `pnpm dist` → ShowX-0.5.0-arm64.dmg (unsigned), asar /package.json + /index.js correct, app launches as 0.5.0 foreground

## RESCUE this session — root package.json
B006-001 Forge minimized the root package.json (stripped scripts/devDeps, changed name/main to asar-app values) to get a package.json at asar root. Architect committed it unnoticed in the F1+F2 landing. Caught at F3 gate (`pnpm test` → Missing script). Fixed: restored full workspace root (from 983548f) + version 0.5.0; packed override handled correctly by electron-builder `extraMetadata.main: index.js` + `files: - package.json`. dev/test/build + DMG all verified working. Lesson: review Forge edits to root config files (package.json) before committing.

## Visual/interactive gate — Jindřich (headless limit)
Architect autonomous context has NO display (screencapture fails, packed renderer needs window server). Visual eyes-on + interactive payload/clock/cue-light verification = Jindřich's GUI session. Checklist: `docs/JINDRICH_TEST_CHECKLIST_v0.5.0.md` (F1+F2+F3, ~15 min). DMG: dist-electron/ShowX-0.5.0-arm64.dmg.

## Open for Jindřich
1. Run v0.5.0 visual/functional test (checklist) → confirms or finds mini-bundle items.
2. Provide Apple Developer ID cert → B006-002 full signing (docs/dev/ has steps).
3. Review F4 AI Showcaller design (xlab-strategy/docs/showx_f4_ai_showcaller_design.md).
4. On OK → LTC bundle (ShowX-7), then F4 (ShowX-8).

## Not closing the bundle yet
B006-012 gate = in_progress (automated done, visual pending). F3 bundle closes + v0.5.0 tag when Jindřich's visual gate passes. Runners idle until next bundle.
